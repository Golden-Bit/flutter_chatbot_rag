import 'package:flutter/material.dart';

class AppColors {
  /// Brand blue ENAC
  static const primary = Color(0xFF005E95);

  /// Tinta molto chiara del primary per highlight di righe selezionate
  /// (alternativa a 0xFFE8F0FE usata prima)
  static const primaryTint = Color(0xFFE5F1F7);

  /// Se ti serve un “hover” o un blu un po’ più chiaro
  static const primaryLight = Color(0xFF0A76B0);
}
